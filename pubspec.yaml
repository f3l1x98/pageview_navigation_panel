name: pageview_navigation_panel
description: Navigation panel with currentPage indicator for PageView
version: 1.0.0
author: Felix Müller
homepage: https://github.com/f3l1x98/pageview_navigation_panel

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
